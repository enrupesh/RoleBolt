NODE_ENV=production
PORT=8080

# MongoDB connection string
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# ── Authentication ──────────────────────────────────────────────────────────
# JWT signing secret — generate with:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=

# Resend API key — for sending verification emails
RESEND_API_KEY=re_...

# From address for auth emails (must be from a verified Resend domain)
SMTP_FROM_NAME=Rolebolt
SMTP_FROM_EMAIL=noreply@rolebolt.tech

# Public URL of the frontend — used to build verification email links
FRONTEND_URL=https://rolebolt.tech

# ── AI keys ─────────────────────────────────────────────────────────────────
# Mesh API gateway key (resume scoring, copilot, form scoring, etc.)
GOOGLEM_API_KEY=rsk_...

# Google Gemini API key (JD generation)
GOOGLE_API_KEY=

# Nvidia NIM API key (AI fallback)
GOOGLEN_API_KEY=

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated allowed origins
CORS_ORIGIN=https://rolebolt.tech
