Here's what I'm observing:

I created a new job, and as you can see, the job was created successfully. The job description also looked reasonably good.

Then I applied with a resume that was a strong match for the job. In that case, the match score was generated correctly, and the overall results looked good.

After that, I applied with another resume that was not a good match for the same job. Instead of generating a low score, it showed **"Scoring Unavailable."** I then clicked **Retry** several times, waiting a little between each attempt. After a few retries, the score was finally generated correctly.

However, even when the score was generated, the **job brief** and **job details** were poor. They were incomplete, low quality, and not what I expected. The generated content doesn't seem to meet the standard the application is supposed to provide.

I also noticed something else. Earlier, when you checked the Mesh API dashboard, there were only two requests recorded. Later, I checked again and saw that the total had increased to four requests. However, after that, the request count never increased again, even though I continued creating jobs and applying to them.

This is making me wonder whether my website is actually calling the Mesh API for every operation. If the API is not being called, then how are the job description, brief, details, and score being generated? Some content is clearly being generated, but it doesn't seem to be consistently coming from the AI.

Could you please investigate the entire request flow from the frontend to the backend and confirm:

* Whether every AI-related feature is actually making a request to the Mesh API.
* Why some requests appear in the dashboard while others do not.
* Whether any fallback logic, caching, mock data, or backend-generated content is being used instead of the AI.
* Why the score sometimes returns "Scoring Unavailable" and only works after multiple retries.
* Why the generated brief and job details are of much lower quality than expected.

Please trace the complete pipeline and identify exactly what is happening. Right now, the behavior is inconsistent, and it's difficult to determine whether the AI is actually powering these features or if another part of the backend is generating the responses.
