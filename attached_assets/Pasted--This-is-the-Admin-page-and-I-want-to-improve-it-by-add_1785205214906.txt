> This is the **Admin** page, and I want to improve it by adding much better transparency around our AI infrastructure.
>
> Here's what I want:
>
> We currently use three AI providers:
>
> * **Google M API** (formerly Mesh API)
> * **Google API** (Gemini)
> * **NVIDIA API**
>
> The Admin panel should clearly show:
>
> 1. **Where each AI API is being used** throughout the platform.
>
>    * Where Google M API is used.
>    * Where Google API (Gemini) is used.
>    * Where NVIDIA API is used.
> 2. **All fallback models** configured for each provider.
>
>    * All fallback models for Google M API.
>    * All fallback models for Google API (Gemini).
>    * All fallback models for NVIDIA API.
> 3. **Live model status.**
>
>    * Show which Google M API model is currently active.
>    * Show which Google API (Gemini) model is currently active.
>    * Show which NVIDIA API model is currently active.
> 4. **Fallback transparency.**
>
>    * If Google M API becomes unavailable, clearly indicate that requests have automatically fallen back to NVIDIA API.
>    * Show exactly **which NVIDIA model** is currently handling those requests.
>    * Likewise, if Google API (Gemini) becomes unavailable, indicate that it has fallen back to NVIDIA API and display the exact NVIDIA model that is currently serving those requests.
>
> In other words, I don't just want to see whether an API is online or offline. I want complete visibility into the routing logic:
>
> * Which provider is currently being used.
> * Which model is serving requests.
> * Which provider failed (if any).
> * Which fallback provider took over.
> * Which fallback model is active.
> * Whether the system is operating normally or in fallback mode.
>
> The goal is to make the Admin dashboard highly transparent, so it's always easy to understand how the AI infrastructure is behaving in real time.
>
> You can also suggest additional improvements that would make this dashboard even more informative and professional, similar to what large production platforms provide for monitoring AI services.
